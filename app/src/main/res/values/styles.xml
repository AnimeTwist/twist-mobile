<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Light application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorSecondary">@color/colorAccent</item>

        <item name="colorOnBackground">@color/colorText</item>
        <item name="android:colorBackground">@color/colorBackground</item>

        <item name="colorOnSurface">@color/colorText</item>
        <item name="colorSurface">@color/colorCardBackground</item>

        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/colorBackground</item>

        <item name="fontFamily">@font/circularstd_medium</item>
        <item name="cardBackgroundColor">@color/colorCardBackground</item>
        <item name="android:navigationBarColor" tools:targetApi="lollipop">@color/colorBackground
        </item>
    </style>

    <style name="AppTheme.Dark">
        <item name="cardBackgroundColor">@color/colorBackground</item>
    </style>

    <style name="ATSpinner" parent="@style/Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateOnly">true</item>
        <item name="android:indeterminateDrawable">@drawable/ic_animated_spinner</item>
        <item name="android:indeterminateBehavior">repeat</item>
        <item name="android:minHeight">24dp</item>
        <item name="android:minWidth">24dp</item>
    </style>
</resources>
